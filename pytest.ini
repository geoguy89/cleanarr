[pytest]
testpaths = tests
addopts = -ra
markers =
    e2e: end-to-end run through faster-whisper (slow, downloads tiny.en)
    ffmpeg: needs ffmpeg and ffprobe on PATH
    ui: drives the page in Chromium through Playwright
