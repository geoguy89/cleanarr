fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pyyaml==6.0.2
faster-whisper==1.1.1
# huggingface_hub reaches for requests when it downloads a model, and
# faster-whisper does not pull it in on its own.
requests==2.32.3
